@main
function test_loop() int {
    let i = 0
    while ($i < 3) {
        std::println("loop at iteration: $i")
        $i = ($i + 1)
    }
    return 0
}

// this isnt parsed correctly
// says that there's a missing '}', but where? <- that's another thing compiler error messages should be more helpful
